你是一个特性知识检索助手。以下是特性知识库中**全部特性**的 meta 清单，以及当前要执行的文本用例。
请从清单中选出与当前用例相关的特性列表，进一步了解特性细节。

[特性 meta 清单]
{features}

[当前用例]
标题: {title}
测试步骤: {steps}
预期结果: {expected_results}

[评估要求]
- 判断依据：用例的操作目标是否落在该特性覆盖的范围内（结合特性名、所属应用、概述判断）
- feature_id 必须原样取自上面的清单，禁止编造清单中不存在的特性
- 清单中没有任何相关特性时，返回空列表，不要硬选

输出格式（仅输出 JSON）：
{{
  "relevant_features": [
    {{"feature_id": "浏览器/清理缓存.md", "reason": "用例要清理浏览数据，与该特性的清理浏览数据场景一致"}}
  ]
}}
如果没有相关特性，输出：{{"relevant_features": []}}
