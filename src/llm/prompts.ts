/**
 * ========================================
 * 🤖 LLM Prompt 配置文件
 * 在这里编辑所有 LLM 的提示词
 * ========================================
 */

// ========================================
// 1. Flow 匹配阶段的 Prompt
// ========================================
export const FLOW_MATCH_PROMPT = `你是一个专业的工作流匹配专家。请根据用户的自然语言请求，从可用的工作流中选择最匹配的那一个。

⚠️ 超级重要规则！！！
1. WORKFLOW 是参数化的模板！示例值只是录制时的演示值，不是固定的！
   - 例如：如果一个 workflow 可以"在搜索框输入文本"，那么它可以输入 ANY 文本！
   - 如果 workflow 搜索的是"11"，但用户想搜索"王者荣耀"，这就是完美匹配！
   - 忽略示例值，只看操作类型和域名！

2. 必须精准匹配！
   - 匹配操作类型：用户想搜索 → 选有 input 操作的 flow
   - 匹配域名：用户想搜百度 → 选 tags 包含 baidu 的 flow
   - 匹配场景：用户想登录 → 选有登录相关步骤的 flow

所有可用的工作流（FLOWS）：
{{FLOW_LIST}}

执行步骤：
1. 仔细分析用户的请求，理解用户想做什么
2. 找出最匹配的 flow：
   - 首先看域名/网站是否匹配
   - 然后看操作类型是否匹配（搜索/点击/输入/登录等）
   - 最后看场景是否匹配
3. 从用户查询中提取参数值（要输入/搜索的内容）
4. 给出你的匹配理由

必须返回 JSON 格式！！！
{
  "selectedFlowId": "最匹配的 flow 的 id，如果完全不匹配返回 null",
  "confidenceScore": 匹配度 0.0 到 1.0,
  "extractedArgs": {
    "inputValue": "从用户查询中提取的要输入/搜索的值"
  },
  "reasoning": "详细的匹配理由，说明为什么选这个 flow"
}`;


// ========================================
// 2. Flow 分析（从会话中提取）的 Prompt
// ========================================
export const FLOW_ANALYSIS_PROMPT = `你是一个智能工作流提取专家。从用户可能包含噪音的浏览器操作序列中，提取核心任务。

用户意图: {{TASK_DESCRIPTION}}

⚠️ ⚠️ ⚠️ 绝对严格规则：
1. 绝对不要修改 CSS 选择器！必须使用录制时的精确选择器！
2. 🔥🔥🔥 必须保留输入值！精确复制原始值！
3. 🔥🔥🔥 必须保留所有 "keydown Enter" 步骤！保持为独立步骤！
4. 跳过录制时的错误、重试、弯路（如：输错密码→验证失败→重新输入，只保留一次正确的输入）
5. 去掉弹窗、无关点击等冗余操作

你的响应必须是有效的 JSON 对象，结构如下：
{
  "name": "简洁清晰的工作流名称，聚焦核心任务",
  "description": "这个工作流完成的功能",
  "tags": ["域名", "操作类型", "场景"],
  "analysis": {
    "detectedIntent": "用户真正想做的任务",
    "distractionsRemoved": ["被移除的冗余操作列表"],
    "optimizations": ["做了哪些优化"]
  },
  "steps": [
    {
      "action": "navigate|click|input|select|keydown",
      "description": "这个步骤的清晰中文描述",
      "targetSelector": "录制时的精确 CSS 选择器 - 绝对不要修改！",
      "value": "如果是输入类型，保留原始值",
      "key": "按键类型，如：'Enter'",
      "conditions": ["重要注意事项"]
    }
  ]
}`;


// ========================================
// 3. Playwright 代码生成 + 步骤优化 Prompt
// ========================================
export const CODE_GENERATION_PROMPT = `你是一个浏览器自动化专家。请基于提供的原始步骤，按用户需求优化。

🚀 输出要求（必须严格遵守！）：
1. 必须返回标准 JSON 格式！
2. steps 数组：优化后的步骤列表
3. code 字段：完整的 Playwright 代码

步骤优化规则（超级重要！）：
🔥🔥🔥 必须移除所有弹窗、公告、提示框相关的点击步骤！
- 例如：selector 包含 popup、modal、alert、notice、ann 等关键词
- 这些元素不一定每次都出现，会导致执行失败！
- 去除所有重复、冗余的操作
- 步骤描述使用清晰的中文，如："点击登录按钮"、"输入邮箱地址"
- 保留原来的值，不要修改

用户需求: {{USER_QUERY}}

原始步骤列表：
{{STEPS_JSON}}

请返回 JSON 格式：
{
  "steps": [
    {
      "action": "click/input/navigate",
      "selector": "CSS选择器",
      "value": "输入的值（如果是 input 类型）",
      "description": "人类能看懂的步骤描述"
    }
  ],
  "code": "完整的 Playwright 代码"
}`;


// ========================================
// 4. Playwright 代码生成规则说明
// ========================================
export const CODE_RULES = `
Playwright 代码生成规则：
1. 点击操作前必须添加 page.waitForSelector('selector', { state: 'visible' })
2. 弹窗处理使用 try-catch，不要阻塞主流程
3. 代码必须可以直接复制运行！不允许使用占位符，必须是真实数据
4. 代码开头用 // === 任务名称 === 作为标题
`;
