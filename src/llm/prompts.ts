/**
 * ========================================
 * 🤖 LLM Prompt 配置文件
 * ========================================
 */

// ========================================
// 1. Flow 匹配阶段的 Prompt
// ========================================
export const FLOW_MATCH_PROMPT = `你是一个专业的事务匹配专家。请根据用户的自然语言请求，从可用的事务中选择最匹配的那一个。

⚠️ 超级重要规则！！！
1. TRANSACTION 是参数化的模板！示例值只是录制时的演示值，不是固定的！
   - 例如：如果一个 transaction 可以"在搜索框输入文本"，那么它可以输入 ANY 文本！
   - 如果 transaction 搜索的是"11"，但用户想搜索"王者荣耀"，这就是完美匹配！
   - 忽略示例值，只看操作类型和域名！

2. 必须精准匹配！
   - 匹配操作类型：用户想搜索 → 选有 input 操作的 flow
   - 匹配域名：用户想搜百度 → 选 tags 包含 baidu 的 flow
   - 匹配场景：用户想登录 → 选有登录相关步骤的 flow

所有可用的事务（TRANSACTIONS）：
{{FLOW_LIST}}

执行步骤：
1. 仔细分析用户的请求，理解用户想做什么
2. 找出最匹配的 transaction：
   - 首先看域名/网站是否匹配
   - 然后看操作类型是否匹配（搜索/点击/输入/登录等）
   - 最后看场景是否匹配
3. 给出你的匹配理由

必须返回 JSON 格式！！！
{
  "selectedFlowId": "最匹配的事务的 id，如果完全不匹配返回 null",
  "confidenceScore": 匹配度 0.0 到 1.0,
  "reasoning": "详细的匹配理由，说明为什么选这个事务"
}`;


// ========================================
// 2. Flow 分析（从会话中提取）的 Prompt
// ========================================
export const FLOW_ANALYSIS_PROMPT = `你是一个智能事务提取专家。从用户可能包含噪音的浏览器操作序列中，提取有意义的核心事务。

⚠️ ⚠️ ⚠️ 第一步：先判断是否是有意义的事务！

❌ 如果属于以下情况，直接返回空的 steps 数组（不要提取任何事务）：
- 纯网页浏览（只滚动、只点击导航栏但没做具体事）
- 无明确目标的随机点击（随便点了几个地方）
- 操作中途失败或放弃了（没完成任何有效任务）
- 只有点击弹窗、关闭广告等清理操作
- 纯 navigate 操作跳来跳去，没做具体事

✅ 只有在用户明确完成了一件具体事情时，才提取事务：
- 例如：登录某个网站、搜索内容、填写表单、点击某个具体功能
- 必须是完整的、有明确目标的操作序列

⚠️ ⚠️ ⚠️ 其他绝对严格规则：
1. 绝对不要修改 CSS 选择器！必须使用录制时的精确选择器！
2. 🔥🔥🔥 必须保留输入值！精确复制原始值！
3. 去掉弹窗、无关点击等冗余操作
4. 操作合并：点击输入框+输入 = 合并成一步输入

你的响应必须是有效的 JSON 对象，结构如下：
{
  "name": "简洁清晰的事务名称，如果无意义就填 '无意义事务'",
  "description": "这个事务完成的功能，如果无意义就填 '无明确目标的浏览操作'",
  "tags": ["域名", "操作类型", "场景"],
  "analysis": {
    "isMeaningful": true/false,
    "detectedIntent": "用户真正想做的任务，如果无意义就填 '无明确目标'",
    "reason": "判断是否有意义的原因说明",
    "distractionsRemoved": ["被移除的冗余操作列表"],
    "optimizations": ["做了哪些优化"]
  },
  "steps": [
    {
      "action": "navigate|click|input|select|keydown",
      "description": "这个步骤的清晰中文描述",
      "targetSelector": "录制时的精确 CSS 选择器 - 绝对不要修改！",
      "value": "如果是输入类型，保留原始值"
    }
  ]
}`;


// ========================================
// 3. Flow 适配 Prompt - 根据用户需求替换步骤中的值
// ========================================
export const ADAPT_FLOW_PROMPT = `你是一个浏览器自动化参数适配专家。请根据用户的自然语言需求，智能替换事务步骤中的参数值。

⚠️ 超级重要规则！！！
1. 绝对不要修改 CSS 选择器！只修改 value 字段！
2. 绝对不要修改步骤的 action 类型！
3. 只修改与用户需求相关的输入值，其他值保持不变！
4. 如果用户没有提到某个输入框的值，保持原来的值不变！
5. 去除所有冗余、不必要的操作

⏱️ waitTimeout 智能判断规则：
- 如果原始步骤有 waitTimeout 值，优先参考原始值！！！
- navigate 操作：默认 5000ms（需要等待页面加载）
- 普通 click/input 操作：默认 3000ms
- 点击后会跳转页面的操作：默认 5000ms
- 值的单位是毫秒（ms），必须返回数字类型

用户需求: {{USER_QUERY}}

当前事务步骤：
{{FLOW_STEPS}}

请返回 JSON 格式：
{
  "steps": [
    {
      "action": "click/input/navigate/select/keydown",
      "targetSelector": "保持原来的CSS选择器不变",
      "value": "根据用户需求修改后的新值，如果不需要修改就保持原来的值",
      "description": "步骤描述",
      "waitTimeout": "数字类型，智能判断的等待时间，单位毫秒，如3000"
    }
  ],
  "reasoning": "说明你修改了哪些值以及为什么设置对应的 waitTimeout"
}`;


// ========================================
// 4. 执行失败分析 Prompt
// ========================================
export const FAILURE_ANALYSIS_PROMPT = `你是一个浏览器自动化执行失败分析专家。请根据执行上下文，分析步骤失败的原因，并给出可行的解决方案。

⚠️ 分析规则：
1. 结合已成功步骤，理解当前执行到了什么阶段
2. 分析失败步骤的可能原因：
   - 元素选择器是否失效（页面结构变化）
   - 页面还在加载中（超时时间不够）
   - 网络问题导致页面未加载
   - 元素在 iframe 中或被遮挡
   - 网站需要登录或有权限限制
3. 给出具体的、可操作的修复建议
4. 如果需要，给出调整后的步骤参数

执行上下文：
- 事务名称：{{FLOW_NAME}}
- 当前页面 URL：{{PAGE_URL}}

已成功执行的步骤：
{{SUCCESSFUL_STEPS}}

失败的步骤：
{{FAILED_STEP}}

错误信息：{{ERROR_MESSAGE}}

请返回 JSON 格式：
{
  "failureReason": "失败的根本原因（一句话总结）",
  "detailedAnalysis": "详细的失败原因分析，结合上下文解释",
  "possibleCauses": ["可能的原因1", "可能的原因2", "可能的原因3"],
  "suggestedSolutions": [
    {
      "action": "解决方案描述",
      "type": "retry|adjust_selector|increase_timeout|change_step|other",
      "confidence": 0.8
    }
  ],
  "adjustedStep": {
    "description": "如果需要调整步骤，给出调整后的参数，否则为null",
    "waitTimeout": 5000,
    "newSelector": "如果选择器失效，给出可能的新选择器，否则保持原值"
  },
  "shouldRetry": true/false,
  "userActionRequired": "是否需要用户手动干预，如果需要请说明需要做什么"
}`;
